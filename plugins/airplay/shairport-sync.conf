general = {
  password = "lmpkaihl@AP1";
  volume_max_db = 5.0 ;
};

sessioncontrol = {
  #run_this_before_play_begins = "/usr/src/airplay-scripts/avron.sh";
  #run_this_after_play_ends = "/usr/src/airplay-scripts/avroff.sh";
  run_this_before_entering_active_state = "/home/ludovic/avron.sh"; // make sure the application has executable permission. If it's a script, include the shebang (#!/bin/...) on the first line
  run_this_after_exiting_active_state = "/home/ludovic/avroff.sh"; // make sure the application has executable permission. If it's a script, include the shebang (#!/bin/...) on the first line
};